import React from 'react';
import logo from '../assets/logo.png';

const Logo = () => {
    return (
        <img src={logo} alt="Logistics UK Logo" className="logo-image" />
    );
};

export default Logo;
